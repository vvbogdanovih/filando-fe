services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.prod
    container_name: filando-frontend
    restart: unless-stopped
    env_file: .env.prod
    ports:
      - "3000:3000"